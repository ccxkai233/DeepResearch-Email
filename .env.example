# ===============================================================
# Environment Variables Template - .env.example
# ===============================================================
#
# 使用说明:
# 1. 将此文件复制为 .env (命令: cp .env.example .env)。
# 2. 在新创建的 .env 文件中填入您的真实凭证。
# 3. .env 文件已被 .gitignore 忽略，不会被提交到代码仓库。
#

# --- AI 提供商 API 密钥 ---

# OpenAI / 兼容服务
# 如果您使用 OpenAI 或任何兼容其 API 格式的服务，请在此处填入密钥。
OPENAI_API_KEY="sk-..."

# [可选] 自定义 OpenAI 兼容服务的 Base URL
# 示例: OPENAI_BASE_URL="http://localhost:1234/v1"
OPENAI_BASE_URL=""

# Google AI Studio (Gemini)
# 如果您选择使用 "gemini" 提供商，请在此处填入您的 Google AI Studio API 密钥。
GOOGLE_API_KEY="your-google-api-key"

# [可选] 自定义 Gemini 兼容服务的 Base URL
# 示例: GOOGLE_BASE_URL="http://localhost:8080/v1beta"
GOOGLE_BASE_URL=""


# --- 搜索提供商 API 密钥 ---

# Tavily
# 如果您选择使用 "tavily" 搜索提供商，请在此处填入您的 Tavily API 密钥。
TAVILY_API_KEY="tvly-..."


# --- 邮件发送配置 ---

# 收件人邮箱地址
EMAIL_TO="your-email@example.com"

# SMTP 服务器信息 (以 QQ 邮箱为例)
# 您需要根据您使用的邮箱服务商进行修改。
# 例如，Gmail, Outlook, 163 等都有不同的 SMTP 地址和端口。
EMAIL_HOST="smtp.qq.com"
EMAIL_PORT="465"
EMAIL_USER="your-qq-email@qq.com"
EMAIL_PASS="your-qq-app-specific-password" # 重要：这里通常是邮箱服务商生成的“应用专用密码”，而不是您的邮箱登录密码。